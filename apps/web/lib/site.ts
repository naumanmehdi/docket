// ---------------------------------------------------------------------------
// SITE — web-side brand constants. Client components import THIS, never
// `@apprank/core` (core bundles pg, which is server-only). Keep name/domain in
// sync with packages/core/src/brand.ts (the agent/server-facing source).
// ---------------------------------------------------------------------------
export const SITE = {
  name: "AppRank",
  domain: "apprank.vercel.app",
};
