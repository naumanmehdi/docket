{
  "name": "docket",
  "version": "0.1.0",
  "private": true,
  "description": "docket — the agent-first catalog across ideas, apps, MCPs & skills (rundocket.xyz).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "next": "16.2.12",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
